# AI module environment template.
# Copy to .env and fill in real values. .env is gitignored.

# Flask server
PORT=8000

# Database engine: postgres (default) or mysql
DB_ENGINE=postgres

# Database connection
DB_HOST=postgres
DB_PORT=5432
DB_NAME=medical_supply_chain
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false
